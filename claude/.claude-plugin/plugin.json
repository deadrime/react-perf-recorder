{
  "name": "react-perf-recorder",
  "description": "Record why a React app re-renders and prove a fix with numbers: a skill, an agent and the MCP server of react-perf-recorder.",
  "mcpServers": {
    "react-perf-recorder": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/../dist/cli.js", "mcp"]
    }
  }
}
