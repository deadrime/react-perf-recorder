node_modules
dist
.agent-artifacts
test-results
playwright-report
*.tsbuildinfo

# Working notes: useful to keep, not to publish
CONTEXT.md

# The React 19 half of the test matrix; its lockfile is committed, its install is not
test/react19/node_modules
dist-pages
