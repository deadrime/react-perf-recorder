import { memo } from 'react';

// Default-exported without a name of its own: the build names it after the file.
export default memo(() => <strong className="workspace" data-testid="workspace">Design team</strong>);
